{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "concurrently --names \"Laravel,Next.js\" --prefix-colors \"blue,green\" \"php artisan serve\" \"npm run dev --prefix frontend\"",
        "build": "npm run build --prefix frontend",
        "frontend": "npm run dev --prefix frontend",
        "backend": "php artisan serve"
    },
    "devDependencies": {
        "concurrently": "^9.0.1"
    }
}
